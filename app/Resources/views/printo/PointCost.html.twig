{% import 'homeplanet/macro/commun.twig' as commun %}
<span class="badge badge-aspect-{{o.type}} badge-pill {{ 
	param.conversation is defined 
	and param.character is defined
	and not o.validate( [ param.conversation, param.character ] ) 
	? 'disabled' }}"
	>
{{ o.value }}
</span>


{# {{ commun.credit( oDemand.getPrice() ) }} #}